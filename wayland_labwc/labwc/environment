# Тут лежат переменные окружения, они же меняют раскладку, а так же можно курсоры прописать

# This allows xdg-desktop-portal-wlr to function (e.g. for screen-recording)
XDG_CURRENT_DESKTOP=wlroots
XDG_SESSION_DESKTOP=wlroots
XDG_SESSION_TYPE=wlroots

# Настройка раскладки клавиатуры:
XKB_DEFAULT_LAYOUT=us,ru
XKB_DEFAULT_OPTIONS=grp:alt_shift_toggle

# Рыжий стартует под вялым, а не через иксы
MOZ_ENABLE_WAYLAND=1

# Курсоры
XCURSOR_THEME=breeze_cursors

# Какое то отключение аппаратных курсоров, яхз что это
WLR_NO_HARDWARE_CURSORS=1

# Что то для нормальной работы жабоприложений
_JAVA_AWT_WM_NONREPARENTING=1
